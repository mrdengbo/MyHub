1、在X86系统下，下面函数的值是多少，为什么？
		 Int main()
		 {
					Int a[4] = {1, 2, 3, 4};
					Int *ptr1 = (int *)(&a + 1);
					Int *ptr2 = (int *)((int)a + 1);
					
					Printf(“%x, %x”, ptr1[-1], *ptr2);
					
					Return 0;
		 }
		 
2、题目如下：
  Int a[5][5];
  Int (*p)[4];
  P = a;
  问&p[4][2] - &a[4][2]的结果为多少？为什么？
  
3、题目如下：
   int a[3];
   a[0]=0; a[1]=1; a[2]=2;
   int *p, *q;
   p=a;
   q=&a[2];
   问a[q-p]的结果为多少
   

 